# Minimised from celery, where `should_terminate = None` and
# `should_stop = None` were reported. None is a legitimate initial value for
# anything, and a flag being set up is not a name lying about what it holds.
should_terminate = None
is_ready = None
count = None
