# The corpus: async-heavy Python packages, pinned so a scan is reproducible.
#
# Chosen for async density rather than raw download count. The first scan of
# this project's local Python library found no async bugs at all across 3,003
# files - correct, but uninformative, because almost none of that code is async.
# A checker about async code has to be pointed at async code.
#
# Refresh with: python corpus/fetch.py

aiohttp==3.13.2
httpx==0.28.1
starlette==0.49.1
fastapi==0.120.4
uvicorn==0.38.0
anyio==4.11.0
websockets==15.0.1
aiofiles==24.1.0
asyncpg==0.30.0
redis==6.4.0
tornado==6.5.2
sanic==25.3.0
quart==0.20.0
aiosqlite==0.21.0
aiobotocore==2.24.2
motor==3.7.1
celery==5.5.3
strawberry-graphql==0.290.1
litestar==2.19.0
aiokafka==0.12.0
